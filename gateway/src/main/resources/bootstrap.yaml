spring:
  application:
    name: gateway
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
#          url-expression: "http://172.30.0.1:53/api/v1/namespaces/streama/services/clone/proxy/"
          url-expression: "http://clone.streama.svc.api-starter-us-east-1-openshift-com:443.local:80"
management:
  endpoints:
    web:
      exposure:
        include: ["*"]
  endpoint:
    health:
      show-details: always
logging:
  level:
    org.springframework: debug