spring:
  application:
    name: clone
  cloud:
    kubernetes:
      config:
        sources:
        - name: ${spring.application.name}
management:
  security:
    enabled: false
  endpoints:
    web:
      exposure:
        include: ["*"]
  endpoint:
    health:
      show-details: always