spring:
  application:
    name: clone
  cloud:
    kubernetes:
      config:
        name: streama
        namespace: streama
        paths:
          - clone
management:
  endpoints:
    web:
      exposure:
        include: ["*"]
  endpoint:
    health:
      show-details: always