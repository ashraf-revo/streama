spring:
  application:
    name: clone
  cloud:
    kubernetes:
      config:
        namespace: streama
        sources:
        - namespace: streama
          name: streama
        name: streama
management:
  endpoints:
    web:
      exposure:
        include: ["*"]
  endpoint:
    health:
      show-details: always