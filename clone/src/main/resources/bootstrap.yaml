spring:
  application:
    name: clone
  cloud:
    discovery:
      client:
        simple:
          local:
            instance-id: ${kubernetes.podName}
management:
  endpoints:
    web:
      exposure:
        include: ["*"]
  endpoint:
    health:
      show-details: always