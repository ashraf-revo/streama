spring:
  application:
    name: clone
  cloud:
    kubernetes:
      config:
        name: ${spring.application.name}
        sources:
        - name: ${spring.application.name}
        namespace: streama
management:
  endpoints:
    web:
      exposure:
        include: ["*"]
  endpoint:
    health:
      show-details: always