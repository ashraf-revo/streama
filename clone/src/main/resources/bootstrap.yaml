spring:
  application:
    name: clone
  cloud:
    kubernetes:
      config:
        namespace: streama
        sources:
        - namespace: streama
          name: clone
        name: streama
management:
  endpoints:
    web:
      exposure:
        include: ["*"]
  endpoint:
    health:
      show-details: always