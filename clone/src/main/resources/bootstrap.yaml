spring:
  application:
    name: clone
  cloud:
    kubernetes:
      config:
        sources:
        - name: other
        - name: ${spring.application.name}
management:
  endpoints:
    web:
      exposure:
        include: ["*"]
  endpoint:
    health:
      show-details: always