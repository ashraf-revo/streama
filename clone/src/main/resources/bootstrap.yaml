spring:
  application:
    name: clone
  cloud:
    kubernetes:
      config:
        sources:
        - name: streama
        namespace: clone
management:
  endpoints:
    web:
      exposure:
        include: ["*"]
  endpoint:
    health:
      show-details: always