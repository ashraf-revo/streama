spring:
  application:
    name: clone
  cloud:
    kubernetes:
      reload:
        enabled: true
        mode: polling
        period: 5000
management:
  endpoints:
    web:
      exposure:
        include: ["*"]
  endpoint:
    health:
      show-details: always