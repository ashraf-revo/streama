spring:
  application:
    name: clone
  cloud:
    kubernetes:
      config:
        sources:
        - name: clone
#          namespace: streama
        namespace: streama
management:
  endpoints:
    web:
      exposure:
        include: ["*"]
  endpoint:
    health:
      show-details: always