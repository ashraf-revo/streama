spring:
  application:
    name: clone
  cloud:
    kubernetes:
      config:
        sources:
        - namespace: ${spring.application.name}
          name: streama
management:
  endpoints:
    web:
      exposure:
        include: ["*"]
  endpoint:
    health:
      show-details: always