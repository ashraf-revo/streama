spring:
  application:
    name: clone
  cloud:
    kubernetes:
      config:
        sources:
        - name: ${spring.application.name}
management:
  endpoints:
    web:
      exposure:
        include: ["*"]
  endpoint:
    health:
      show-details: always